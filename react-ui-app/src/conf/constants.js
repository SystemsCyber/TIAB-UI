// src/config.js
const config = {
  API_BASE_URL: "http://10.211.55.13:5000", // Base endpoint for API calls
  ENGINE_ENDPOINT: "/api/engines", // Endpoint for engines data
  COMMANDS_ENDPOINT: "/api/cmd", // Endpoint for commands
  // Add more constants as needed
};

export default config;
